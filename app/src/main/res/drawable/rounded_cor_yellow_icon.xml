<!-- res/drawable/combined_drawable.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- Background Drawable -->
  <item android:drawable="@drawable/rounded_corner_yellow" />

  <!-- Overlay Drawable aligned to the right -->
  <item
      android:drawable="@drawable/exclamation_icon"
      android:gravity="right"
      android:right="30dp"
      android:bottom="10dp"
      android:top="10dp"
      android:width="10dp"
      android:height="30dp"/> <!-- Adjust the right inset as needed -->

</layer-list>
