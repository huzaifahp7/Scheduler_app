<!-- res/drawable/combined_drawable.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- Background Drawable -->
  <item android:drawable="@drawable/rounded_corner_green" />

  <!-- Overlay Drawable aligned to the right -->
  <item
      android:drawable="@drawable/thumbs_up_icon"
      android:gravity="right"
      android:right="20dp"
      android:bottom="10dp"
      android:top="10dp"
      android:width="40dp"
      android:height="30dp" /> <!-- Adjust the right inset as needed -->

</layer-list>
